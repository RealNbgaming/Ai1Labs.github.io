<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=yes, viewport-fit=cover">
    <title>Ai1 - Intelligent AI Assistant</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            -webkit-tap-highlight-color: transparent;
        }

        :root {
            --ai1-blue: #0066ff;
            --ai1-purple: #6c5ce7;
            --ai1-pink: #ec4899;
            --ai1-glow: 0 0 30px rgba(0, 102, 255, 0.3);
            --ai1-gradient: linear-gradient(135deg, #0066ff, #6c5ce7, #ec4899);
            
            --bg-primary: #ffffff;
            --bg-secondary: #f8fafc;
            --bg-sidebar: #ffffff;
            --text-primary: #0f172a;
            --text-secondary: #475569;
            --border-color: #e2e8f0;
            --hover-bg: #f1f5f9;
            --card-bg: #ffffff;
            --code-bg: #f1f5f9;
            
            --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
            --shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1);
            --shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.1);
            --shadow-xl: 0 20px 25px -5px rgb(0 0 0 / 0.1);
            
            --radius-sm: 0.5rem;
            --radius-md: 0.75rem;
            --radius-lg: 1rem;
            --radius-xl: 1.5rem;
            --radius-2xl: 2rem;
        }

        [data-theme="dark"] {
            --bg-primary: #0f172a;
            --bg-secondary: #1e293b;
            --bg-sidebar: #1e293b;
            --text-primary: #f8fafc;
            --text-secondary: #94a3b8;
            --border-color: #334155;
            --hover-bg: #334155;
            --card-bg: #1e293b;
            --code-bg: #0f172a;
        }

        body {
            background-color: var(--bg-primary);
            color: var(--text-primary);
            height: 100vh;
            width: 100vw;
            overflow: hidden;
            display: flex;
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            transition: background-color 0.3s, color 0.3s;
        }

        .app-container {
            display: flex;
            width: 100%;
            height: 100%;
            overflow: hidden;
        }

        /* ============== AI1 SIDEBAR ============== */
        .sidebar {
            width: 280px;
            background-color: var(--bg-sidebar);
            display: flex;
            flex-direction: column;
            height: 100%;
            border-right: 1px solid var(--border-color);
            position: relative;
            overflow: hidden;
            transition: background-color 0.3s;
        }

        .sidebar::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: var(--ai1-gradient);
        }

        .sidebar-header {
            padding: 24px 20px;
            border-bottom: 1px solid var(--border-color);
        }

        .logo-container {
            display: flex;
            align-items: center;
            gap: 12px;
            margin-bottom: 24px;
        }

        .ai1-icon {
            width: 48px;
            height: 48px;
            background: var(--ai1-gradient);
            border-radius: 16px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
            font-weight: 700;
            color: white;
            box-shadow: var(--shadow-lg);
            position: relative;
            overflow: hidden;
        }

        .ai1-icon::before {
            content: 'Ai';
            font-size: 20px;
            font-weight: 600;
        }

        .ai1-icon::after {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: linear-gradient(45deg, transparent, rgba(255,255,255,0.2), transparent);
            transform: rotate(45deg);
            animation: shimmer 3s infinite;
        }

        @keyframes shimmer {
            0% { transform: translateX(-100%) rotate(45deg); }
            100% { transform: translateX(100%) rotate(45deg); }
        }

        .logo-text {
            font-size: 28px;
            font-weight: 700;
            background: var(--ai1-gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .new-chat-btn {
            width: 100%;
            padding: 14px 20px;
            background: var(--ai1-gradient);
            border: none;
            border-radius: var(--radius-xl);
            color: white;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 12px;
            font-size: 15px;
            font-weight: 500;
            transition: all 0.3s;
        }

        .new-chat-btn:hover {
            transform: translateY(-2px);
            box-shadow: var(--shadow-lg);
        }

        .chat-history {
            flex-grow: 1;
            overflow-y: auto;
            padding: 20px;
        }

        .history-title {
            color: var(--text-secondary);
            font-size: 12px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 16px;
            padding-left: 8px;
        }

        .chat-history-item {
            padding: 12px 16px;
            border-radius: var(--radius-lg);
            cursor: pointer;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 4px;
            font-size: 14px;
            color: var(--text-secondary);
            transition: all 0.2s;
            border: 1px solid transparent;
        }

        .chat-history-item:hover {
            background-color: var(--hover-bg);
            color: var(--text-primary);
            border-color: var(--border-color);
            transform: translateX(4px);
        }

        .chat-history-item.active {
            background: var(--ai1-gradient);
            color: white;
        }

        .chat-history-date {
            font-size: 11px;
            color: var(--text-secondary);
            margin-left: 8px;
        }

        .chat-history-item.active .chat-history-date {
            color: rgba(255, 255, 255, 0.8);
        }

        .delete-chat {
            color: var(--text-secondary);
            padding: 4px;
            border-radius: 4px;
            opacity: 0;
            transition: opacity 0.2s;
        }

        .chat-history-item:hover .delete-chat {
            opacity: 1;
        }

        .delete-chat:hover {
            color: #ef4444;
            background-color: rgba(239, 68, 68, 0.1);
        }

        .sidebar-footer {
            padding: 20px;
            border-top: 1px solid var(--border-color);
        }

        .user-profile {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 8px 12px;
            border-radius: var(--radius-lg);
            background-color: var(--hover-bg);
        }

        .user-avatar {
            width: 40px;
            height: 40px;
            background: var(--ai1-gradient);
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 600;
            color: white;
            font-size: 16px;
        }

        .user-info {
            flex-grow: 1;
        }

        .user-name {
            font-size: 14px;
            font-weight: 600;
            color: var(--text-primary);
        }

        .user-status {
            font-size: 12px;
            color: var(--text-secondary);
            display: flex;
            align-items: center;
            gap: 4px;
        }

        .status-dot {
            width: 8px;
            height: 8px;
            background: #10b981;
            border-radius: 50%;
            display: inline-block;
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); opacity: 1; }
            50% { transform: scale(1.2); opacity: 0.8; }
        }

        /* ============== MAIN CHAT AREA ============== */
        .chat-container {
            flex-grow: 1;
            display: flex;
            flex-direction: column;
            height: 100%;
            background-color: var(--bg-primary);
            position: relative;
            overflow: hidden;
        }

        .chat-header {
            padding: 20px 32px;
            border-bottom: 1px solid var(--border-color);
            display: flex;
            align-items: center;
            justify-content: space-between;
            background-color: var(--bg-primary);
            backdrop-filter: blur(10px);
            position: sticky;
            top: 0;
            z-index: 10;
        }

        .chat-header-left {
            display: flex;
            align-items: center;
            gap: 16px;
        }

        .ai1-badge {
            display: flex;
            align-items: center;
            gap: 8px;
            background-color: var(--hover-bg);
            padding: 6px 16px;
            border-radius: var(--radius-xl);
            border: 1px solid var(--border-color);
        }

        .ai1-badge i {
            color: var(--ai1-blue);
            font-size: 12px;
        }

        .ai1-badge span {
            font-size: 13px;
            color: var(--text-secondary);
        }

        .chat-header h1 {
            font-size: 20px;
            font-weight: 600;
            background: var(--ai1-gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .chat-header-right {
            display: flex;
            gap: 12px;
        }

        .header-btn {
            width: 40px;
            height: 40px;
            border-radius: var(--radius-lg);
            background-color: var(--hover-bg);
            border: 1px solid var(--border-color);
            color: var(--text-secondary);
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s;
        }

        .header-btn:hover {
            border-color: var(--ai1-blue);
            color: var(--ai1-blue);
            transform: translateY(-2px);
        }

        /* Chat Messages */
        .chat-messages {
            flex-grow: 1;
            overflow-y: auto;
            padding: 32px;
            display: flex;
            flex-direction: column;
            gap: 24px;
            scroll-behavior: smooth;
        }

        /* Welcome Screen */
        .welcome-message {
            text-align: center;
            max-width: 800px;
            margin: 40px auto;
            animation: fadeInUp 0.6s ease;
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .welcome-icon {
            width: 120px;
            height: 120px;
            margin: 0 auto 24px;
            background: var(--ai1-gradient);
            border-radius: 40px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 60px;
            color: white;
            box-shadow: var(--shadow-xl);
            animation: float 3s ease-in-out infinite;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
        }

        .welcome-message h2 {
            font-size: 36px;
            font-weight: 700;
            margin-bottom: 12px;
            background: var(--ai1-gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .welcome-subtitle {
            font-size: 16px;
            color: var(--text-secondary);
            margin-bottom: 40px;
        }

        .suggestion-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 16px;
            max-width: 600px;
            margin: 0 auto;
        }

        .suggestion-card {
            background-color: var(--card-bg);
            border: 1px solid var(--border-color);
            border-radius: var(--radius-lg);
            padding: 20px;
            text-align: left;
            cursor: pointer;
            transition: all 0.3s;
            position: relative;
            overflow: hidden;
        }

        .suggestion-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 3px;
            background: var(--ai1-gradient);
            transform: translateX(-100%);
            transition: transform 0.3s;
        }

        .suggestion-card:hover {
            transform: translateY(-4px);
            border-color: var(--ai1-blue);
            box-shadow: var(--shadow-lg);
        }

        .suggestion-card:hover::before {
            transform: translateX(0);
        }

        .suggestion-icon {
            font-size: 24px;
            margin-bottom: 12px;
            background: var(--ai1-gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .suggestion-title {
            font-weight: 600;
            margin-bottom: 4px;
            color: var(--text-primary);
        }

        .suggestion-subtitle {
            font-size: 13px;
            color: var(--text-secondary);
        }

        /* Message styling with streaming animation */
        .message {
            display: flex;
            gap: 20px;
            max-width: 800px;
            margin: 0 auto;
            width: 100%;
            padding: 8px 0;
            animation: messageSlide 0.3s ease;
        }

        @keyframes messageSlide {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .message-avatar {
            width: 40px;
            height: 40px;
            border-radius: 14px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 600;
            flex-shrink: 0;
            font-size: 20px;
            background: var(--ai1-gradient);
            color: white;
        }

        .message-content {
            flex-grow: 1;
            padding-top: 8px;
            font-size: 15px;
            line-height: 1.7;
            color: var(--text-primary);
        }

        /* Streaming text animation */
        .streaming-text {
            animation: fadeInStream 0.02s ease;
        }

        @keyframes fadeInStream {
            from {
                opacity: 0;
                transform: translateY(2px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .message-content pre {
            background-color: var(--code-bg);
            padding: 20px;
            border-radius: var(--radius-lg);
            overflow-x: auto;
            margin: 16px 0;
            border: 1px solid var(--border-color);
            border-left: 4px solid var(--ai1-blue);
        }

        .message-content code {
            font-family: 'Fira Code', monospace;
            background-color: var(--code-bg);
            padding: 3px 8px;
            border-radius: 6px;
            font-size: 0.9em;
            border: 1px solid var(--border-color);
        }

        .message-content blockquote {
            border-left: 4px solid var(--ai1-blue);
            padding-left: 20px;
            margin: 16px 0;
            color: var(--text-secondary);
            font-style: italic;
        }

        /* Image Gallery */
        .image-gallery {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin: 20px 0;
        }

        .image-card {
            background-color: var(--card-bg);
            border-radius: var(--radius-lg);
            overflow: hidden;
            border: 1px solid var(--border-color);
            transition: all 0.3s;
        }

        .image-card:hover {
            transform: translateY(-4px);
            border-color: var(--ai1-blue);
            box-shadow: var(--shadow-lg);
        }

        .image-container {
            position: relative;
            width: 100%;
            height: 250px;
            background: linear-gradient(45deg, var(--bg-secondary), var(--hover-bg));
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .generated-image {
            max-width: 100%;
            max-height: 250px;
            object-fit: contain;
            cursor: pointer;
            border-radius: var(--radius-md);
        }

        .image-actions {
            display: flex;
            padding: 12px;
            gap: 8px;
            justify-content: center;
            border-top: 1px solid var(--border-color);
        }

        .image-action-btn {
            flex: 1;
            padding: 8px 12px;
            border-radius: var(--radius-md);
            background-color: var(--hover-bg);
            border: 1px solid var(--border-color);
            color: var(--text-primary);
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 6px;
            font-size: 12px;
            transition: all 0.2s;
        }

        .image-action-btn:hover {
            border-color: var(--ai1-blue);
            color: var(--ai1-blue);
        }

        /* Streaming typing indicator */
        .streaming-indicator {
            display: flex;
            gap: 6px;
            padding: 12px 0;
        }

        .streaming-dot {
            width: 8px;
            height: 8px;
            background: var(--ai1-gradient);
            border-radius: 50%;
            animation: streamingPulse 1s infinite ease-in-out;
        }

        .streaming-dot:nth-child(1) { animation-delay: 0s; }
        .streaming-dot:nth-child(2) { animation-delay: 0.15s; }
        .streaming-dot:nth-child(3) { animation-delay: 0.3s; }

        @keyframes streamingPulse {
            0%, 80%, 100% { transform: scale(0.6); opacity: 0.5; }
            40% { transform: scale(1); opacity: 1; }
        }

        /* Chat input */
        .chat-input-container {
            padding: 24px 32px;
            max-width: 800px;
            margin: 0 auto;
            width: 100%;
            position: relative;
            background-color: var(--bg-primary);
            border-top: 1px solid var(--border-color);
        }

        .input-wrapper {
            position: relative;
            display: flex;
            border-radius: var(--radius-xl);
            overflow: hidden;
            border: 2px solid var(--border-color);
            background-color: var(--card-bg);
            transition: all 0.3s;
            box-shadow: var(--shadow-sm);
        }

        .input-wrapper:focus-within {
            border-color: var(--ai1-blue);
            box-shadow: 0 0 0 3px rgba(0, 102, 255, 0.1);
        }

        .input-wrapper input {
            width: 100%;
            padding: 18px 24px;
            border: none;
            background: transparent;
            color: var(--text-primary);
            font-size: 15px;
            outline: none;
        }

        .input-wrapper input::placeholder {
            color: var(--text-secondary);
        }

        .input-actions {
            display: flex;
            gap: 8px;
            padding: 0 8px 0 0;
            align-items: center;
        }

        .input-actions button {
            width: 44px;
            height: 44px;
            border-radius: 22px;
            background: transparent;
            border: none;
            color: var(--text-secondary);
            cursor: pointer;
            transition: all 0.2s;
            font-size: 18px;
        }

        .input-actions button:hover {
            color: var(--ai1-blue);
            transform: scale(1.1);
            background-color: var(--hover-bg);
        }

        .disclaimer {
            text-align: center;
            font-size: 12px;
            color: var(--text-secondary);
            margin-top: 16px;
        }

        .disclaimer a {
            color: var(--ai1-blue);
            text-decoration: none;
            font-weight: 500;
        }

        /* Mobile */
        @media screen and (max-width: 768px) {
            .sidebar {
                position: fixed;
                top: 0;
                left: -100%;
                width: 85%;
                max-width: 320px;
                height: 100%;
                z-index: 1000;
                transition: left 0.3s ease;
            }

            .sidebar.open {
                left: 0;
            }

            .sidebar-overlay {
                position: fixed;
                top: 0;
                left: 0;
                right: 0;
                bottom: 0;
                background: rgba(0, 0, 0, 0.5);
                z-index: 999;
                display: none;
                backdrop-filter: blur(4px);
            }

            .sidebar-overlay.open {
                display: block;
            }

            .mobile-menu-btn {
                display: flex !important;
            }

            .chat-header h1 {
                display: none;
            }

            .suggestion-grid {
                grid-template-columns: 1fr;
            }

            .chat-messages {
                padding: 16px;
            }

            .chat-input-container {
                padding: 16px;
            }
        }

        .mobile-menu-btn {
            display: none;
            align-items: center;
            justify-content: center;
            width: 40px;
            height: 40px;
            border-radius: var(--radius-md);
            background-color: var(--hover-bg);
            border: 1px solid var(--border-color);
            color: var(--text-secondary);
            cursor: pointer;
            font-size: 18px;
        }

        /* Scrollbar */
        ::-webkit-scrollbar {
            width: 8px;
            height: 8px;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
        }

        ::-webkit-scrollbar-thumb {
            background: var(--ai1-blue);
            border-radius: 4px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #4d8cff;
        }
    </style>
</head>
<body data-theme="light">
    <div class="sidebar-overlay" id="sidebarOverlay"></div>

    <!-- Main App Container -->
    <div class="app-container" id="appContainer">
        <!-- Ai1 Sidebar -->
        <aside class="sidebar" id="sidebar">
            <div class="sidebar-header">
                <div class="logo-container">
                    <div class="ai1-icon"></div>
                    <div class="logo-text">Ai1</div>
                </div>
                <button class="new-chat-btn" id="newChat">
                    <i class="fas fa-plus"></i>
                    <span>New Conversation</span>
                </button>
            </div>
            
            <div class="history-title">Recent Chats</div>
            <div class="chat-history" id="chatHistory"></div>

            <div class="sidebar-footer">
                <div class="user-profile">
                    <div class="user-avatar">
                        <i class="fas fa-user"></i>
                    </div>
                    <div class="user-info">
                        <div class="user-name">Guest User</div>
                        <div class="user-status">
                            <span class="status-dot"></span>
                            <span>Online</span>
                        </div>
                    </div>
                </div>
            </div>
        </aside>

        <!-- Main chat area -->
        <main class="chat-container">
            <div class="chat-header">
                <div class="chat-header-left">
                    <button class="mobile-menu-btn" id="mobileMenuBtn">
                        <i class="fas fa-bars"></i>
                    </button>
                    
                    <div class="ai1-badge">
                        <i class="fas fa-bolt"></i>
                        <span>Ai1 Active</span>
                    </div>
                    <h1>Intelligent Assistant</h1>
                </div>
                <div class="chat-header-right">
                    <button class="header-btn" id="themeToggle">
                        <i class="fas fa-moon"></i>
                    </button>
                </div>
            </div>

            <div class="chat-messages" id="chatMessages">
                <div class="welcome-message">
                    <div class="welcome-icon">
                        <i class="fas fa-robot"></i>
                    </div>
                    <h2>Welcome to Ai1</h2>
                    <p class="welcome-subtitle">Your intelligent AI assistant, powered by advanced AI. Ask me anything!</p>
                    <div class="suggestion-grid">
                        <div class="suggestion-card" onclick="document.getElementById('userInput').value='Make me a picture of a beautiful sunset'; sendMessage()">
                            <div class="suggestion-icon"><i class="fas fa-image"></i></div>
                            <div class="suggestion-title">Make a picture</div>
                            <div class="suggestion-subtitle">Generate an image</div>
                        </div>
                        <div class="suggestion-card" onclick="document.getElementById('userInput').value='Who made you?'; sendMessage()">
                            <div class="suggestion-icon"><i class="fas fa-user"></i></div>
                            <div class="suggestion-title">Who made you?</div>
                            <div class="suggestion-subtitle">Noe Benitez</div>
                        </div>
                        <div class="suggestion-card" onclick="document.getElementById('userInput').value='What is the capital of France?'; sendMessage()">
                            <div class="suggestion-icon"><i class="fas fa-globe"></i></div>
                            <div class="suggestion-title">Capital of France</div>
                            <div class="suggestion-subtitle">Geography</div>
                        </div>
                        <div class="suggestion-card" onclick="document.getElementById('userInput').value='Explain quantum computing in simple terms'; sendMessage()">
                            <div class="suggestion-icon"><i class="fas fa-atom"></i></div>
                            <div class="suggestion-title">Quantum computing</div>
                            <div class="suggestion-subtitle">Simple explanation</div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="chat-input-container">
                <div class="input-wrapper">
                    <input type="text" id="userInput" placeholder="Ask Ai1 anything..." autocomplete="off">
                    <div class="input-actions">
                        <button id="sendButton" title="Send message"><i class="fas fa-paper-plane"></i></button>
                    </div>
                </div>
                <p class="disclaimer">⚡ Created by <a href="#">Noe Benitez</a> at Ai1 Labs • Intelligent AI responses</p>
            </div>
        </main>
    </div>

    <script>
        // ============== AI1 INTELLIGENT API CONFIGURATION ==============
        // Using Google's Gemini API for intelligence, fully branded as Ai1
        const AI1_API_KEY = "AIzaSyAQJJ8_P0oSL73-9DGm43en7gA_NYU0VV4";
        const AI1_MODEL = "gemini-3.1-flash-lite-preview";
        const AI1_API_URL = `https://generativelanguage.googleapis.com/v1beta/models/${AI1_MODEL}:generateContent?key=${AI1_API_KEY}`;

        // ============== INSTANT RESPONSES FOR CREATOR QUESTIONS ==============
        const CREATOR_RESPONSES = {
            'who made you': `### 👤 Created by Noe Benitez!\n\n**Noe Benitez** is my creator and the founder of **Ai1 Labs**! 🎉\n\n### 🏆 About My Creator:\n- **Latin Grammy Winner** for "Los Animales"\n- **2 Wins** at the Latin Grammys\n- Founder of Ai1 Labs\n\n[View Noe Benitez on LatinGRAMMY.com](https://www.latingrammy.com/en/artists/noe-benitez/23955-01)`,
            
            'who created you': `### 👤 Created by Noe Benitez!\n\n**Noe Benitez** is my creator and founder of **Ai1 Labs**!`,
            
            'who is your creator': `### 👤 Noe Benitez created me!\n\nHe's a Latin Grammy winner and founder of Ai1 Labs.`,
            
            'noe benitez': `### 👤 About Noe Benitez\n\n**Noe Benitez** is a Latin Grammy winner and the founder of Ai1 Labs! 🏆`,
            
            'what company': `### 🏢 Ai1 Labs\n\nI was created at **Ai1 Labs**, founded by Noe Benitez.`,
            
            'which company': `### 🏢 Ai1 Labs\n\nThat's my home! Ai1 Labs, founded by Noe Benitez.`,
            
            'ai1 labs': `### 🏢 Yes, Ai1 Labs!\n\nThat's where I was born! Founded by Noe Benitez.`
        };

        // ============== IMAGE GENERATION ==============
        function getImageUrl(prompt) {
            const cleanPrompt = prompt.replace(/make me a picture of|make me an image of|generate a picture of|create a picture of/i, '').trim() || 'beautiful scenery';
            const seed = Date.now();
            return `https://image.pollinations.ai/prompt/${encodeURIComponent(cleanPrompt)}?width=512&height=512&nologo=true&seed=${seed}`;
        }

        // ============== STREAMING TEXT ANIMATION ==============
        async function streamTextToMessage(content, messageElement) {
            const contentDiv = messageElement.querySelector('.message-content');
            contentDiv.innerHTML = '';
            
            // Split into words and stream
            const words = content.split(/(\s+)/);
            let currentText = '';
            
            for (let i = 0; i < words.length; i++) {
                currentText += words[i];
                contentDiv.innerHTML = marked.parse(currentText);
                messageElement.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
                
                // Dynamic delay for natural typing effect
                const delay = words[i].length > 10 ? 30 : 20;
                await new Promise(resolve => setTimeout(resolve, delay));
            }
        }

        // ============== GET AI1 INTELLIGENT RESPONSE ==============
        async function getAi1IntelligentResponse(userPrompt, imageData = null) {
            try {
                const response = await fetch(AI1_API_URL, {
                    method: "POST",
                    headers: {
                        "Content-Type": "application/json"
                    },
                    body: JSON.stringify({
                        contents: [{
                            parts: imageData ? [
                                { text: userPrompt },
                                { inlineData: { mimeType: "image/jpeg", data: imageData } }
                            ] : [{ text: userPrompt }]
                        }],
                        generationConfig: {
                            temperature: 0.7,
                            maxOutputTokens: 2048,
                            topP: 0.95,
                            topK: 40
                        }
                    })
                });

                if (response.ok) {
                    const data = await response.json();
                    return data.candidates?.[0]?.content?.parts?.[0]?.text || "I'm here to help!";
                }
                return "I'm having trouble connecting. Please try again.";
            } catch (error) {
                console.error("Ai1 API Error:", error);
                return "I'm having trouble connecting. Please check your internet connection.";
            }
        }

        // ============== THEME TOGGLE ==============
        const themeToggle = document.getElementById('themeToggle');
        const html = document.documentElement;
        
        function toggleTheme() {
            const currentTheme = html.getAttribute('data-theme') || 'light';
            const newTheme = currentTheme === 'light' ? 'dark' : 'light';
            html.setAttribute('data-theme', newTheme);
            themeToggle.querySelector('i').className = newTheme === 'light' ? 'fas fa-moon' : 'fas fa-sun';
            localStorage.setItem('ai1-theme', newTheme);
        }

        const savedTheme = localStorage.getItem('ai1-theme') || 'light';
        html.setAttribute('data-theme', savedTheme);
        themeToggle.querySelector('i').className = savedTheme === 'light' ? 'fas fa-moon' : 'fas fa-sun';
        themeToggle.addEventListener('click', toggleTheme);

        // ============== CHAT FUNCTIONALITY ==============
        document.addEventListener('DOMContentLoaded', function() {
            const userInput = document.getElementById('userInput');
            const sendButton = document.getElementById('sendButton');
            const chatMessages = document.getElementById('chatMessages');
            const newChatBtn = document.getElementById('newChat');
            const chatHistory = document.getElementById('chatHistory');
            const mobileMenuBtn = document.getElementById('mobileMenuBtn');
            const sidebar = document.getElementById('sidebar');
            const sidebarOverlay = document.getElementById('sidebarOverlay');
            
            let chats = JSON.parse(localStorage.getItem('ai1-intelligent-chats')) || {};
            let currentChatId = Date.now().toString();
            let isStreaming = false;

            // Mobile sidebar
            if (mobileMenuBtn) {
                mobileMenuBtn.addEventListener('click', () => {
                    sidebar.classList.toggle('open');
                    sidebarOverlay.classList.toggle('open');
                });
            }
            if (sidebarOverlay) {
                sidebarOverlay.addEventListener('click', () => {
                    sidebar.classList.remove('open');
                    sidebarOverlay.classList.remove('open');
                });
            }

            function saveChats() {
                localStorage.setItem('ai1-intelligent-chats', JSON.stringify(chats));
            }

            // Check for creator questions
            function isCreatorQuestion(message) {
                const lowerMsg = message.toLowerCase().trim();
                for (const key of Object.keys(CREATOR_RESPONSES)) {
                    if (lowerMsg.includes(key)) {
                        return true;
                    }
                }
                return false;
            }

            function getCreatorResponse(message) {
                const lowerMsg = message.toLowerCase().trim();
                for (const [key, response] of Object.entries(CREATOR_RESPONSES)) {
                    if (lowerMsg.includes(key)) {
                        return response;
                    }
                }
                return null;
            }

            // Check for image generation
            function isImageRequest(message) {
                const lowerMsg = message.toLowerCase().trim();
                return lowerMsg.includes('make me a picture') || 
                       lowerMsg.includes('make me an image') ||
                       lowerMsg.includes('generate a picture') ||
                       lowerMsg.includes('create a picture');
            }

            async function handleImageGeneration(message) {
                const imageUrl = getImageUrl(message);
                return `### 🎨 Here's your Ai1-generated image!\n\n` +
                       `<div class="image-gallery">` +
                       `<div class="image-card">` +
                       `<div class="image-container">` +
                       `<img src="${imageUrl}" class="generated-image" alt="Generated image" loading="lazy" onerror="this.parentElement.innerHTML='<div style=\'padding: 20px; text-align: center;\'>Image generation in progress. <a href=\'${imageUrl}\' target=\'_blank\'>Click here to view</a></div>'">` +
                       `</div>` +
                       `<div class="image-actions">` +
                       `<button class="image-action-btn" onclick="window.open('${imageUrl}', '_blank')">` +
                       `<i class="fas fa-external-link-alt"></i> View Full Size` +
                       `</button>` +
                       `<button class="image-action-btn" onclick="navigator.clipboard.writeText('${imageUrl}')">` +
                       `<i class="fas fa-link"></i> Copy Link` +
                       `</button>` +
                       `</div>` +
                       `</div>` +
                       `</div>\n\n` +
                       `Want another? Just say "Make me a picture of..."!`;
            }

            renderChatHistory();
            
            if (chats[currentChatId]?.messages?.length > 0) {
                renderMessages(chats[currentChatId].messages);
                document.querySelector('.welcome-message')?.remove();
            }

            sendButton.addEventListener('click', sendMessage);
            userInput.addEventListener('keypress', (e) => {
                if (e.key === 'Enter') sendMessage();
            });
            
            newChatBtn.addEventListener('click', startNewChat);

            async function sendMessage() {
                if (isStreaming) return;
                
                const message = userInput.value.trim();
                if (!message) return;

                document.querySelector('.welcome-message')?.remove();

                addMessage('user', marked.parse(message));
                userInput.value = '';
                
                // Show streaming indicator
                const streamingDiv = document.createElement('div');
                streamingDiv.className = 'message assistant-message';
                streamingDiv.id = 'streamingIndicator';
                streamingDiv.innerHTML = `
                    <div class="message-avatar"><i class="fas fa-robot"></i></div>
                    <div class="message-content streaming-indicator">
                        <div class="streaming-dot"></div>
                        <div class="streaming-dot"></div>
                        <div class="streaming-dot"></div>
                    </div>
                `;
                chatMessages.appendChild(streamingDiv);
                chatMessages.scrollTop = chatMessages.scrollHeight;
                
                isStreaming = true;

                try {
                    let response;
                    
                    // Check for creator questions first (instant)
                    if (isCreatorQuestion(message)) {
                        response = getCreatorResponse(message);
                        streamingDiv.remove();
                        const messageDiv = addMessage('assistant', '');
                        await streamTextToMessage(response, messageDiv);
                    }
                    // Check for image requests
                    else if (isImageRequest(message)) {
                        response = await handleImageGeneration(message);
                        streamingDiv.remove();
                        addMessage('assistant', marked.parse(response));
                    }
                    // Get intelligent response from API
                    else {
                        response = await getAi1IntelligentResponse(message);
                        streamingDiv.remove();
                        const messageDiv = addMessage('assistant', '');
                        await streamTextToMessage(response, messageDiv);
                    }
                    
                    // Save to history
                    if (!chats[currentChatId]) {
                        chats[currentChatId] = {
                            id: currentChatId,
                            title: message.substring(0, 30) + (message.length > 30 ? '...' : ''),
                            messages: [],
                            createdAt: new Date().toISOString(),
                            updatedAt: new Date().toISOString()
                        };
                    }
                    
                    chats[currentChatId].messages.push(
                        { role: 'user', content: message },
                        { role: 'assistant', content: response }
                    );
                    chats[currentChatId].updatedAt = new Date().toISOString();
                    
                    saveChats();
                    renderChatHistory();
                    
                } catch (error) {
                    console.error("Error:", error);
                    streamingDiv.remove();
                    const errorMsg = "I encountered an error. Please try again.";
                    const messageDiv = addMessage('assistant', '');
                    await streamTextToMessage(errorMsg, messageDiv);
                } finally {
                    isStreaming = false;
                }
            }

            function addMessage(role, content) {
                const messageDiv = document.createElement('div');
                messageDiv.className = `message ${role}-message`;
                messageDiv.innerHTML = `
                    <div class="message-avatar"><i class="fas fa-${role === 'user' ? 'user' : 'robot'}"></i></div>
                    <div class="message-content">${content}</div>
                `;
                chatMessages.appendChild(messageDiv);
                chatMessages.scrollTop = chatMessages.scrollHeight;
                return messageDiv;
            }
            
            function renderMessages(messages) {
                while (chatMessages.children.length > 1) {
                    chatMessages.removeChild(chatMessages.lastChild);
                }
                document.querySelector('.welcome-message')?.remove();
                
                if (messages?.length > 0) {
                    messages.forEach(msg => {
                        const messageDiv = document.createElement('div');
                        messageDiv.className = `message ${msg.role}-message`;
                        messageDiv.innerHTML = `
                            <div class="message-avatar"><i class="fas fa-${msg.role === 'user' ? 'user' : 'robot'}"></i></div>
                            <div class="message-content">${marked.parse(msg.content)}</div>
                        `;
                        chatMessages.appendChild(messageDiv);
                    });
                    chatMessages.scrollTop = chatMessages.scrollHeight;
                }
            }
            
            function renderChatHistory() {
                chatHistory.innerHTML = '';
                
                const sortedChats = Object.values(chats).sort((a, b) => 
                    new Date(b.updatedAt) - new Date(a.updatedAt)
                ).slice(0, 25);
                
                if (sortedChats.length === 0) {
                    chatHistory.innerHTML = '<div style="padding: 20px; color: var(--text-secondary); text-align: center;">No conversations yet</div>';
                    return;
                }
                
                const today = new Date().toDateString();
                const yesterday = new Date(Date.now() - 86400000).toDateString();
                let lastDate = '';
                
                sortedChats.forEach(chat => {
                    const chatDate = new Date(chat.updatedAt);
                    const chatDateStr = chatDate.toDateString();
                    let displayDate = chatDateStr === today ? 'Today' : chatDateStr === yesterday ? 'Yesterday' : chatDate.toLocaleDateString();
                    
                    if (displayDate !== lastDate) {
                        const dateHeader = document.createElement('div');
                        dateHeader.className = 'history-date-group';
                        dateHeader.textContent = displayDate;
                        chatHistory.appendChild(dateHeader);
                        lastDate = displayDate;
                    }
                    
                    const chatItem = document.createElement('div');
                    chatItem.className = `chat-history-item ${chat.id === currentChatId ? 'active' : ''}`;
                    chatItem.innerHTML = `
                        <span style="flex-grow: 1; overflow: hidden; text-overflow: ellipsis;">${chat.title || 'New chat'}</span>
                        <span class="chat-history-date">${new Date(chat.updatedAt).toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' })}</span>
                        <span class="delete-chat"><i class="fas fa-trash"></i></span>
                    `;
                    
                    chatItem.querySelector('.delete-chat').addEventListener('click', (e) => {
                        e.stopPropagation();
                        delete chats[chat.id];
                        saveChats();
                        if (currentChatId === chat.id) startNewChat();
                        else renderChatHistory();
                    });
                    
                    chatItem.addEventListener('click', () => {
                        currentChatId = chat.id;
                        renderMessages(chat.messages);
                        renderChatHistory();
                        if (window.innerWidth <= 768) {
                            sidebar.classList.remove('open');
                            sidebarOverlay.classList.remove('open');
                        }
                    });
                    
                    chatHistory.appendChild(chatItem);
                });
            }
            
            function startNewChat() {
                currentChatId = Date.now().toString();
                
                while (chatMessages.children.length > 1) {
                    chatMessages.removeChild(chatMessages.lastChild);
                }
                
                const welcomeMsg = document.createElement('div');
                welcomeMsg.className = 'welcome-message';
                welcomeMsg.innerHTML = `
                    <div class="welcome-icon">
                        <i class="fas fa-robot"></i>
                    </div>
                    <h2>Welcome to Ai1</h2>
                    <p class="welcome-subtitle">Your intelligent AI assistant, powered by advanced AI. Ask me anything!</p>
                    <div class="suggestion-grid">
                        <div class="suggestion-card" onclick="document.getElementById('userInput').value='Make me a picture of a beautiful sunset'; sendMessage()">
                            <div class="suggestion-icon"><i class="fas fa-image"></i></div>
                            <div class="suggestion-title">Make a picture</div>
                            <div class="suggestion-subtitle">Generate an image</div>
                        </div>
                        <div class="suggestion-card" onclick="document.getElementById('userInput').value='Who made you?'; sendMessage()">
                            <div class="suggestion-icon"><i class="fas fa-user"></i></div>
                            <div class="suggestion-title">Who made you?</div>
                            <div class="suggestion-subtitle">Noe Benitez</div>
                        </div>
                        <div class="suggestion-card" onclick="document.getElementById('userInput').value='What is the capital of France?'; sendMessage()">
                            <div class="suggestion-icon"><i class="fas fa-globe"></i></div>
                            <div class="suggestion-title">Capital of France</div>
                            <div class="suggestion-subtitle">Geography</div>
                        </div>
                        <div class="suggestion-card" onclick="document.getElementById('userInput').value='Explain quantum computing in simple terms'; sendMessage()">
                            <div class="suggestion-icon"><i class="fas fa-atom"></i></div>
                            <div class="suggestion-title">Quantum computing</div>
                            <div class="suggestion-subtitle">Simple explanation</div>
                        </div>
                    </div>
                `;
                chatMessages.appendChild(welcomeMsg);
                renderChatHistory();
                userInput.focus();
            }
        });
    </script>
</body>
</html>
